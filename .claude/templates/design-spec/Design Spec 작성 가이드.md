# **Design Spec 작성 가이드 v1.0**

시안이 아닌 규칙. AI가 이 규칙만으로 디자인 시스템을 준수하는 UI를 생성한다.

* **핵심 질문:** "AI가 이 규칙만으로 디자인 시스템을 준수하는 UI를 생성할 수 있는가?"
* **프로세스 위치:** Stage 4 — Freeze (0.5주)
* **Gate 연결:** Gate 4 (인계 패키지 완성 — 4종 문서 전원 합의)
* **작성:** 디자이너 (Product Spec §7 기반 분리·확장. PM·엔지니어 3자 리뷰 후 확정)
* **데이터 수준:** 확정 (Prototype 결과 + 사용성 테스트 반영)

## **Zero-Design-Handoff 원칙**

이 문서는 "시안"이 아니라 "규칙"이다.

| 기존 방식 | Zero-Design-Handoff |
| ----- | ----- |
| 디자이너 → 50개 시안 (2-3주) → 개발자 | PM → Spec(디자인 규칙) → AI 생성 → 디자이너 QA |
| Figma 파일 전달 | 규칙 문서 전달 |
| 화면별 시안 | 컴포넌트 규칙 + 토큰 |
| 디자이너가 모든 화면 제작 | AI가 규칙 기반으로 생성, 디자이너가 QA |

**Track A (기본 — 이모코그 표준):** shadcn/ui + Tailwind + AI 코딩 도구
**Track B (예외 — 브랜드 강도 높을 때):** Figma 시안 + 이 문서를 Figma 연동 가이드로 활용

## **문서 관계**

| 방향 | 문서 | 읽는 섹션 |
| ----- | ----- | ----- |
| **인풋** | Product Spec §7 (디자인 프롬프트) | IA, 컴포넌트 매핑, 레이아웃 규칙, 토큰 — 이 문서의 시드 |
| **인풋** | Product Spec §3 (사용자 흐름) | Happy Path, Alternative Path → §3 화면 플로우로 확장 |
| **인풋** | Product Spec §2 (사용자 스토리) | 역할·행동 → 화면별 사용자 목표 |
| **아웃풋** | Gate 4 | 인계 패키지 4종 중 1종 (디자이너 소유) |
| **아웃풋** | Stage 5 MVP | AI 코딩 도구(Cursor 등)가 §7 AI 생성 지침을 읽고 UI 컴포넌트 생성 |

## **섹션 맵**

| \# | 섹션명 | 핵심 질문 | S4 분리 시 | 확정 |
| ----- | ----- | ----- | :---: | :---: |
| 1 | 개요 & 디자인 원칙 | 왜 이렇게 보여야 하는가? | **필수** | 확정 |
| 2 | 정보 구조 (IA) | 화면이 어떻게 연결되는가? | **필수** | 확정 |
| 3 | 화면별 레이아웃 명세 | 각 화면에 무엇이 어떻게 배치되는가? | **필수** | 확정 |
| 4 | 컴포넌트 & 인터랙션 규칙 | 어떤 컴포넌트를 어떻게 동작시키는가? | **필수** | 확정 |
| 5 | 디자인 토큰 | 색상·타이포·간격의 기준값은? | **필수** | 확정 |
| 6 | AI 생성 지침 | AI에게 무엇을 어떻게 지시하는가? | **필수** | 확정 |
| 7 | QA 체크리스트 & Gate 4 | 인계 전 디자이너·PM 최종 확인 | 선택 | **확정** |

## **1. 개요 & 디자인 원칙**

이 섹션이 답하는 질문: "이 제품의 디자인이 무엇을 달성해야 하는가? 무엇을 포기하는가?"
인풋: Product Brief §1~§2 (문제 정의, 타겟 고객)

### **1-1. 디자인 개요**

| 항목 | 내용 |
| ----- | ----- |
| **제품명** | \[제품명\] |
| **디자인 목표** | \[이 UI가 달성해야 하는 것. 1문장\] |
| **타겟 사용자** | \[누가 쓰는가. 기술 수준, 맥락\] |
| **플랫폼** | \[Web / Mobile / Desktop — 우선순위 포함\] |
| **디자이너** | \[이름\] |
| **버전** | v1.0 (Stage 4 확정) |

### **1-2. 디자인 원칙 (3가지)**

우선순위 순서로 나열. 충돌 시 상위 원칙 우선.

| 순위 | 원칙 | 의미 | 구체적 적용 예시 |
| ----- | ----- | ----- | ----- |
| 1 | \[원칙명\] | \[1문장 설명\] | \[이 원칙이 적용된 구체적 사례\] |
| 2 | \[원칙명\] | \[1문장 설명\] | \[사례\] |
| 3 | \[원칙명\] | \[1문장 설명\] | \[사례\] |

### **1-3. Non-goals (디자인에서 하지 않는 것)**

| \# | 하지 않는 것 | 이유 |
| ----- | ----- | ----- |
| 1 | \[예: 커스텀 일러스트레이션\] | \[MVP 범위 초과 / AI 생성 불가\] |
| 2 | \[예: 다크모드\] | \[Stage 4 범위 외, 출시 후 추가\] |

---

## **2. 정보 구조 (IA)**

이 섹션이 답하는 질문: "화면이 어떻게 계층화되고 연결되는가?"
인풋: Product Spec §3 (사용자 흐름) + §2 (사용자 스토리)

### **2-1. 화면 계층 구조**

```
[앱/서비스명]
├── [최상위 화면 1] — [진입점/역할]
│   ├── [하위 화면 1-1]
│   └── [하위 화면 1-2]
├── [최상위 화면 2]
│   ├── [하위 화면 2-1]
│   └── [하위 화면 2-2]
└── [공통 레이어: 모달 / 토스트 / 에러 화면]
```

### **2-2. 화면 목록**

| 화면 ID | 화면명 | 접근 경로 | 사용자 목표 | 관련 US |
| ----- | ----- | ----- | ----- | ----- |
| SCR-001 | \[화면명\] | \[앱 진입 → 네비게이션\] | \[이 화면에서 달성하려는 것\] | US-001 |
| SCR-002 | \[화면명\] | \[이전 화면 → 버튼\] | \[목표\] | US-002 |
| SCR-003 | \[화면명\] | \[경로\] | \[목표\] | US-003 |

### **2-3. 네비게이션 패턴**

| 항목 | 결정 | 근거 |
| ----- | ----- | ----- |
| **주 네비게이션** | \[Top Nav / Side Nav / Bottom Tab / 없음\] | \[이유\] |
| **모바일 네비게이션** | \[Bottom Tab / Hamburger\] | \[이유\] |
| **뒤로가기 정책** | \[브라우저 Back / 커스텀 뒤로가기\] | \[이유\] |
| **딥링크 지원** | \[Yes/No — URL 패턴\] | \[이유\] |

---

## **3. 화면별 레이아웃 명세**

이 섹션이 답하는 질문: "각 화면에 무엇이 어디에 배치되는가?"
Product Spec §3 화면 플로우를 화면 단위로 분해·확장한 것.

각 주요 화면에 대해 아래 형식으로 작성. 화면 수가 많으면 P0 화면을 우선 명세.

### **[SCR-001] \[화면명\]**

**목적:** \[이 화면이 사용자에게 달성하게 하는 것\]

#### **레이아웃 구조 (텍스트 와이어프레임)**

```
┌─────────────────────────────────┐
│  [헤더: 로고 | 타이틀 | 액션버튼]  │
├─────────────────────────────────┤
│                                 │
│  [메인 콘텐츠 영역]               │
│  - [요소 1]                     │
│  - [요소 2]                     │
│                                 │
├─────────────────────────────────┤
│  [CTA 버튼: 텍스트]              │
└─────────────────────────────────┘
```

#### **요소 명세**

| 요소 | 컴포넌트 | 상태 | 동작 |
| ----- | ----- | ----- | ----- |
| \[요소명\] | \[shadcn/Button 등\] | \[default/loading/disabled\] | \[클릭 시 → SCR-002\] |
| \[요소명\] | \[컴포넌트\] | \[상태\] | \[동작\] |

### **[SCR-002] \[화면명\]**

**목적:** \[목적\]

#### **레이아웃 구조 (텍스트 와이어프레임)**

```
┌─────────────────────────────────┐
│  [구조 설명]                      │
└─────────────────────────────────┘
```

#### **요소 명세**

| 요소 | 컴포넌트 | 상태 | 동작 |
| ----- | ----- | ----- | ----- |
| \[요소명\] | \[컴포넌트\] | \[상태\] | \[동작\] |

---

## **4. 컴포넌트 & 인터랙션 규칙**

이 섹션이 답하는 질문: "어떤 컴포넌트를 어떻게 쓰고, 상태는 어떻게 표현하는가?"
**기본 라이브러리:** shadcn/ui (Track A). 커스텀이 필요한 경우만 별도 정의.

### **4-1. 컴포넌트 사용 규칙**

| UI 요소 | shadcn 컴포넌트 | 커스텀 여부 | 사용 규칙 |
| ----- | ----- | ----- | ----- |
| 기본 버튼 | Button (variant="default") | 표준 | \[언제 쓰는가\] |
| 보조 버튼 | Button (variant="outline") | 표준 | \[언제 쓰는가\] |
| 텍스트 입력 | Input | 표준 | \[placeholder 규칙\] |
| 드롭다운 | Select | 표준 | \[선택지 3개 초과 시\] |
| 모달 | Dialog | 표준 | \[확인이 필요한 액션에만\] |
| 알림 | Toast | 표준 | \[성공/에러 피드백\] |
| \[커스텀 요소\] | — | 커스텀 | \[왜 커스텀이 필요한가\] |

### **4-2. 상태별 표현 규칙**

| 상태 | 시각 표현 | 적용 컴포넌트 |
| ----- | ----- | ----- |
| **Loading** | Skeleton / Spinner — \[어느 것을 언제\] | \[컴포넌트 목록\] |
| **Empty** | \[빈 상태 메시지 + CTA\] | \[컴포넌트 목록\] |
| **Error** | \[에러 색상(destructive) + 메시지 + 복구 액션\] | \[컴포넌트 목록\] |
| **Disabled** | \[opacity-50 + cursor-not-allowed\] | \[컴포넌트 목록\] |
| **Success** | \[토스트 + 색상 피드백\] | \[컴포넌트 목록\] |

### **4-3. 인터랙션 패턴 규칙**

| 패턴 | 규칙 | 예외 |
| ----- | ----- | ----- |
| **버튼 클릭 피드백** | 클릭 즉시 Loading 상태. 응답 대기 중 재클릭 차단 | \[예외 케이스\] |
| **폼 유효성 검사** | Submit 시점 + 포커스 아웃 시점 둘 다 검사 | \[예외\] |
| **페이지 전환** | 즉시 전환 (Transition 없음). 느린 경우만 Skeleton | \[예외\] |
| **모달 닫기** | ESC키 + 배경 클릭 + X버튼 모두 지원 | \[파괴적 액션은 배경 클릭 닫기 차단\] |
| **에러 복구** | 에러 메시지 + 구체적 복구 방법 명시 | \[이유\] |

---

## **5. 디자인 토큰**

이 섹션이 답하는 질문: "색상·타이포·간격의 기준값은 무엇인가?"
Product Spec §7-4에서 이동·확장. AI 코딩 도구와 CSS 변수로 직접 참조.

### **5-1. 컬러 토큰**

| 토큰명 | 값 | 용도 | Tailwind 클래스 |
| ----- | ----- | ----- | ----- |
| **primary** | \[\#hex\] | CTA 버튼, 강조 | `bg-primary` |
| **primary-foreground** | \[\#hex\] | primary 위 텍스트 | `text-primary-foreground` |
| **secondary** | \[\#hex\] | 보조 요소 | `bg-secondary` |
| **background** | \[\#hex\] | 페이지 배경 | `bg-background` |
| **foreground** | \[\#hex\] | 기본 텍스트 | `text-foreground` |
| **muted** | \[\#hex\] | 비활성 배경 | `bg-muted` |
| **muted-foreground** | \[\#hex\] | 보조 텍스트 | `text-muted-foreground` |
| **destructive** | \[\#hex\] | 에러, 삭제 | `text-destructive` |
| **border** | \[\#hex\] | 구분선 | `border-border` |

### **5-2. 타이포그래피 토큰**

| 토큰명 | 값 | 용도 |
| ----- | ----- | ----- |
| **font-sans** | \[폰트 패밀리\] | 기본 본문 |
| **font-heading** | \[폰트 패밀리\] | 제목 (같을 수도 있음) |
| **text-xs** | 12px / 0.75rem | 캡션, 보조 정보 |
| **text-sm** | 14px / 0.875rem | 보조 본문, 레이블 |
| **text-base** | 16px / 1rem | 기본 본문 |
| **text-lg** | 18px / 1.125rem | 서브타이틀 |
| **text-xl** | 20px / 1.25rem | 섹션 제목 |
| **text-2xl** | 24px / 1.5rem | 페이지 제목 |

### **5-3. 스페이싱 & 레이아웃 토큰**

| 항목 | 값 | 용도 |
| ----- | ----- | ----- |
| **기본 단위** | 4px (Tailwind 기본) | 모든 간격의 배수 |
| **컴포넌트 내부 패딩** | 8px~16px | 버튼, 인풋 등 |
| **섹션 간격** | 24px~48px | 화면 내 섹션 구분 |
| **페이지 패딩** | 16px (모바일) / 24px~32px (데스크톱) | 좌우 여백 |
| **컨테이너 최대 너비** | \[px\] | 콘텐츠 최대 폭 |

### **5-4. 기타 토큰**

| 항목 | 값 | 용도 |
| ----- | ----- | ----- |
| **border-radius-sm** | 4px | 인풋, 태그 |
| **border-radius-md** | 8px | 카드, 버튼 |
| **border-radius-lg** | 12px | 모달, 시트 |
| **shadow-sm** | \[값\] | 카드 |
| **shadow-md** | \[값\] | 모달 |
| **transition** | 150ms ease | 기본 인터랙션 |

---

## **6. AI 생성 지침**

이 섹션이 답하는 질문: "AI 코딩 도구에게 무엇을 어떻게 지시하는가?"
**이 섹션이 Zero-Design-Handoff의 핵심.** AI(Cursor, Lovable, v0 등)가 이 규칙을 읽고 UI를 생성한다.

### **6-1. 기술 스택 지침**

```
[AI에게 주는 지침 - 그대로 복사하여 사용]

기술 스택:
- Framework: Next.js 14 (App Router)
- UI: shadcn/ui + Tailwind CSS
- 아이콘: lucide-react
- 폼: react-hook-form + zod
- 상태: [React Context / Zustand / 기타]

컴포넌트 규칙:
- shadcn/ui 컴포넌트를 최우선으로 사용한다.
- 커스텀 컴포넌트가 필요한 경우만 새로 만든다.
- className에는 Tailwind 유틸리티 클래스만 사용한다.
- CSS 파일을 새로 만들지 않는다.
```

### **6-2. 디자인 토큰 지침**

```
[AI에게 주는 지침]

디자인 토큰 (globals.css의 CSS 변수 사용):
- Primary: [색상값] → Tailwind: bg-primary, text-primary
- Background: [색상값] → Tailwind: bg-background
- ...

레이아웃:
- 페이지 패딩: px-4 (모바일) / px-6 (sm 이상)
- 컨테이너: max-w-[값] mx-auto
- 섹션 간격: space-y-6 또는 gap-6
```

### **6-3. 화면별 생성 지침**

각 P0 화면에 대해 AI가 바로 실행할 수 있는 지침.

#### **[SCR-001] [화면명] 생성 지침**

```
[AI에게 주는 지침]

화면: [화면명]
파일 위치: app/[경로]/page.tsx (또는 components/[이름].tsx)

구조:
- 헤더: [구체적 요소]
- 메인: [구체적 요소]
- CTA: [버튼 텍스트, 동작]

컴포넌트:
- [요소]: shadcn/[컴포넌트] 사용
- [요소]: [구체적 지침]

상태 처리:
- Loading: Skeleton 컴포넌트 사용
- Error: Alert (variant="destructive")
- Empty: [빈 상태 메시지]
```

#### **[SCR-002] [화면명] 생성 지침**

```
[AI에게 주는 지침]

화면: [화면명]
...
```

---

## **7. QA 체크리스트 & Gate 4 자가 점검**

### **디자이너 QA (AI 생성 결과 검증)**

| 항목 | 확인 내용 | 상태 |
| ----- | ----- | ----- |
| **토큰 일관성** | 컬러·타이포·간격이 §5 토큰과 일치하는가? | \[Pass/Fail\] |
| **컴포넌트 일관성** | 동일 역할에 동일 컴포넌트를 쓰는가? | \[Pass/Fail\] |
| **상태 커버리지** | Loading/Error/Empty 모든 상태가 처리되는가? | \[Pass/Fail\] |
| **반응형** | 모바일 브레이크포인트에서 깨지지 않는가? | \[Pass/Fail\] |
| **접근성** | 버튼에 aria-label, 이미지에 alt, 색상 대비 4.5:1 이상 | \[Pass/Fail\] |
| **인터랙션** | §4-2 상태별 규칙이 구현되었는가? | \[Pass/Fail\] |

### **Gate 4 — 인계 전 합의 확인**

| 기준 | 질문 | 판정 |
| ----- | ----- | ----- |
| **완성도** | 모든 P0 화면의 레이아웃 명세가 완성되었는가? | \[Pass/Fail\] |
| **AI 생성 가능** | AI가 §6 지침만 읽고 P0 화면 UI를 생성할 수 있는가? | \[Pass/Fail\] |
| **Product Spec 정합** | Design Spec이 Product Spec §3 플로우와 충돌하지 않는가? | \[Pass/Fail\] |
| **Tech Spec 정합** | 컴포넌트 명세가 Tech Spec 기술 스택과 일치하는가? | \[Pass/Fail\] |
| **3자 합의** | PM · 디자이너 · 엔지니어 전원이 이 문서에 합의했는가? | \[Pass/Fail\] |

* **판정:** \[Freeze 확정 / 수정 후 재검토\]
* **확정자:** \[이름들\] (날짜: YYYY-MM-DD)
* **확정 후 변경:** 변경 대상, 사유, 영향 범위를 기록하고 PM이 판단한다.
